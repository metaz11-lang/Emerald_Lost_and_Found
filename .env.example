# Example environment variables for local development
PORT=3000
NODE_ENV=production

# Demo admin credentials (replace in production, move to real auth)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=emerald2024

# Future database / external service configuration goes here
# DATABASE_URL=
# REDIS_URL=
